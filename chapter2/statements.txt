指针和引用的区别

1. 指针是一个变量，保存的是一个地址值；引用只是一个对象的别名。
2. 可以有cons指针，但是没有const引用。
3. 指针可以多级，引用只能一级。
4. 指针可以为空值，但引用不能为空，定义时必须初始化。
5. 指针指向的对象可以改变，但引用绑定的对象不能改变。
6. 对指针进行sizeof运算,得到的是指针的大小(32位系统为4字节)，对引用进行sizeof运算，得到的值绑定对象的大小。
7. 指针自增(++)、自减(--)和引用自增、自减不同。